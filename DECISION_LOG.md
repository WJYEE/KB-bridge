# 의사결정 문서 (DECISION_LOG.md)

주요 설계 결정과 그 이유를 기록한다. Phase가 진행될 때마다 추가한다.

## Phase 1 (Audit)

### 왜 Next.js + TypeScript + Tailwind + Recharts인가?
기존 프로젝트에 프레임워크가 전혀 없었다(빈 폴더 + 원본 데이터 파일만 존재). 기획
문서(섹션 19)가 신규 프로젝트일 경우의 기본 스택으로 명시했고, 정적 페이지가
많고 서버사이드 집계가 필요한 이 서비스 구조와도 잘 맞아 그대로 채택했다.

### 왜 `202425.csv`를 사용하지 않는가?
`202425.csv`(2024+2025 raw 병합, 227,783행)는 `_2024.csv`(95,081행)와
`_2025.csv`(132,702행)를 합친 것과 행 수·값이 정확히 일치했다. 즉 완전한 중복
데이터였고, 사용할 경우 2024/2025 데이터를 두 번 처리하는 실수로 이어질 위험만
있었다. 전처리 스크립트는 연도별 원본 3개 CSV만 입력 후보로 취급하고, 실제로는
그마저도 아래 이유로 `최종1차데이터셋.xlsx`를 최종 소스로 사용한다.

### 왜 CSV 대신 `최종1차데이터셋.xlsx`를 최종 소스로 쓰는가?
사용자가 이미 취소거래 제외, 자치구명 결측 제외, 건축년도=0 결측 제외까지 마친
"최종 1차" 정제본을 별도로 준비해 두었다(Sheet2, 284,102행). 컬럼 구성도 기획
문서 섹션 4의 핵심 10컬럼과 정확히 일치했다. 원본 CSV부터 우리가 다시 정제하면
동일한 결과를 놓고 이중 작업을 하는 셈이므로, 이 파일을 신뢰하되 전처리
스크립트에서 핵심 컬럼 결측·가격/면적 유효성을 한 번 더 방어적으로 재검증하는
방식을 택했다 (`scripts/preprocess-realestate.ts`). 원본 CSV 3종은 `data/raw/`에
그대로 남겨 데이터 계보(lineage)를 보존한다.

### 왜 Sheet2를 쓰고 Sheet1/Sheet3은 버리는가?
Sheet1은 실제 데이터가 837행뿐이었고(전체 284k행 중 특정 건물 몇 개만 모아둔
스팟체크용 조각), 건축년도가 문자열 `"null"`로 남아있는 등 정제가 끝나지 않은
상태였다. Sheet3은 완전히 빈 시트였다. Sheet2만 컬럼 구성·행 수·결측치 처리
수준에서 "최종" 데이터셋의 특징을 모두 만족했다.

## Phase 2 (Data)

### 왜 평균 대신 중앙값을 사용하는가?
부동산 가격은 고가 거래(펜트하우스, 초고가 단독주택 등)에 의해 오른쪽으로 크게
치우친 분포를 보인다. 실제로 물건금액 최댓값(32억)이 p99(4.2억)의 7배가 넘는다.
평균을 쓰면 극단값 몇 건이 지역 전체의 "평범한 시세"를 왜곡하지만, 중앙값은 이런
왜곡에 강건하다. 기획 문서(섹션 7)도 중앙값을 주요 기준으로 명시했다.

### 왜 층/건물명 결측 행을 제거하지 않았는가?
결측 사유를 확인해보니 100% `단독다가구` 유형과 일치했다 — 무작위 결측이 아니라
주택유형에 따른 구조적 결측(단독/다가구는 층 개념이 없음)이었다. 행을 통째로
버리면 단독다가구 유형 전체가 분석에서 빠지게 되므로, 해당 파생변수만 `null`
처리하고 행은 유지했다.

### 왜 `transactions.json`을 개별 컬럼 객체가 아니라 튜플(배열) 행으로 저장하는가?
284,102행을 `{ "자치구명": "강남구", ... }` 형태의 객체 배열로 저장하면 키 이름이
행마다 반복돼 파일 용량이 커진다(추정 40MB+). 열 순서를 고정한 튜플로 저장하면
동일 데이터가 25.7MB로 줄어든다. 이 파일은 서버 전용(클라이언트 번들에 포함되지
않음)이며, `src/lib/realestate/schema.ts`가 유일한 스키마 정의 역할을 한다.

### 왜 파생변수(평수, ㎡당가격 등)를 JSON에 저장하지 않고 로딩 시점에 계산하는가?
저장 용량을 아낄 수 있고, 무엇보다 면적/가격/연식 구간(bucket) 경계값을 나중에
조정해야 할 때 재전처리 없이 `src/lib/realestate/derive.ts` 코드만 고치면 바로
반영되기 때문이다. 원본 raw 필드 10개만 저장하고 나머지는 순수 함수로 계산한다.

### 왜 `xlsx`(SheetJS) 패키지의 알려진 취약점(프로토타입 오염, ReDoS)을 감수하는가?
`npm audit`에서 high severity로 표시되지만, 이 패키지는 **우리가 직접 관리하는
로컬 파일**(`data/raw/최종1차데이터셋.xlsx`)을 빌드 타임 스크립트에서 1회
파싱하는 용도로만 쓴다. 사용자가 업로드한 임의 파일을 파싱하는 용도로는 절대
사용하지 않으며, 런타임(클라이언트/서버 요청 처리 경로)에도 포함되지 않는다.
공식 수정 버전이 없는 상태이므로, 이 사용 범위를 벗어나는 순간(예: 사용자 xlsx
업로드 기능 추가) 대체 라이브러리를 재검토해야 한다.

## Phase 3 (Product Logic)

### 점수 계산 방식 — 왜 종합점수를 바로 만들지 않고 8개 차원을 먼저 분리했는가?
가격·공간·신축·지역·주택유형·층·시장안정성·거래활성도 8개 점수를 각각 0~100으로
계산한 뒤, 사용자가 입력한 가중치로 가중합해 `totalScore`를 만든다
(`src/lib/recommendation/score.ts`, `recommend.ts`). 이렇게 분리해야 "왜 이 지역이
추천되었는가"를 각 차원별 숫자로 설명할 수 있고(Explainable), 사용자가 가중치를
바꿨을 때 결과가 왜 달라지는지도 그대로 재계산해서 보여줄 수 있다. 하나의 종합
점수만 먼저 만들면 이 설명 가능성을 잃는다.

### 왜 "가격이 싸면 100점"이 아닌가?
가격 적합도(`scorePrice`)는 지역 전체 거래 중 예산 이하 비율(`affordableRatio`,
가중 0.7)과 예산 대비 여유(`budgetHeadroomRatio`, 가중 0.3)를 합성한다. 가장 싼
지역이 아니라 "예산 안에서 선택지가 충분한 지역"을 찾는 것이 서비스 목적이기
때문이다 (기획 Phase 3 §3). 실제로 강남구처럼 비싼 지역도 절대적으로 거래량이
많아 예산 이하 거래 자체는 상당수 존재할 수 있으므로, 단순 "저렴한 지역 1위"와는
다른 결과가 나온다.

### 정규화 방식 — 왜 순수 min-max가 아니라 percentile-clipped min-max/rank-percentile인가?
25개 자치구를 비교 모집단으로 점수를 매기는데, 강남·서초 같은 극단적으로 비싼
지역이 1~2곳만 있어도 순수 min-max는 나머지 지역을 0~30점대로 뭉개버려
변별력을 잃는다. p5~p95 구간으로 클리핑한 뒤 min-max를 적용해 극단값의 영향은
줄이되 중간 지역들의 상대적 차이는 보존했다 (`normalize.ts`의
`clippedMinMaxScore`). 거래 유동성처럼 지역 간 절대값 편차가 매우 큰 지표(강서구
최대 8,809건 vs 용산구 최소 97건, 4~2억 budget 시나리오 기준)에는
rank-percentile(`rankPercentileScore`)을 써서 순위만 보게 했다 — 그래야 면적이
넓은 자치구가 "유동성"에서 항상 유리해지는 문제를 피할 수 있다.

### 최소 표본 수(신뢰도) 기준 — 왜 30 / 10인가?
예산+면적+연식+유형+층 조건을 모두 건 엄격한 시나리오를 실제 데이터로 테스트한
결과(2.5억/30㎡↑/10년↓/오피스텔/특정구 조합), 지역별 표본이 1건~수십 건까지
크게 갈렸고 25개 중 9개 지역은 0건이었다. 반면 예산만 건 완만한 시나리오는 최소
지역도 수백 건이었다. 통계적으로 중앙값이 개별 이상치에 흔들리지 않을 만큼
표본이 있다고 보는 관례적 기준(대표본 근사, n≥30)을 "high", 그 아래 10 이상을
"참고 가능하나 변동성 큼(medium)", 10 미만을 "low", 0건을 "insufficient"로
구분했다 (`recommend.ts`의 `MIN_SAMPLE_HIGH_CONFIDENCE`/`MIN_SAMPLE_MEDIUM_CONFIDENCE`).
`insufficient` 지역은 TOP5 추천에서 제외하고 `alternatives`에만 담는다.

### 시장 안정성 정의 — 왜 "많이 오른 지역"이 아니라 변동계수(CV)인가?
2024~2026 3개년 ㎡당가격 중앙값의 변동계수(표준편차/평균)를 구해, 낮을수록
안정적이라고 본다 (`coefficientOfVariation`, `scoreStability`). 값이 오른
지역이든 내린 지역이든 "변동폭"만 측정하므로 급등 지역에 무조건 높은 점수를
주지 않는다 — 이는 기획 §9에서 명시적으로 요구한 원칙이다. 투자수익률 개념으로
쓰지 않는다는 점도 코드 주석에 남겼다.

### 거래 유동성("실거래 활성도") 정의 — 왜 실제 유동성이라 부르지 않는가?
MVP 데이터에는 지역별 주택 재고량(전체 매물 수)이 없어 "재고 대비 거래 속도"를
계산할 수 없다. 대신 전체 거래건수와 최근연도(2026) 거래건수를 각각
rank-percentile로 변환해 절반씩 반영한 값을 "실거래 활성도"라는 이름으로 쓴다
(기획 §10 지침 그대로 반영). "유동성"이라는 이름이 주는 오해(진짜 매물 회전율)를
피하기 위해 UI 문구에서도 이 이름을 그대로 쓸 예정이다.

### 층 적합도 — 왜 절대 층수로 저/중/고층을 나눴는가?
원본 데이터에 건물 전체 층수가 없어 "30층 건물의 25층"처럼 상대적 위치를 알 수
없다. 건축 실무에서 통상 쓰는 저층(≤3층)/중층(4~9층)/고층(10층 이상) 3단
구분을 절대 층수 기준으로 그대로 채택했고, 반지하/지하(음수 층)와 1층은 저층으로
묶었다 (`derive.ts`의 `getFloorBucket`). 이 기준은 실제 데이터 분포 검증 없이
정한 임의 기준이므로 여기 명시한다 — 추후 층별 실거래 분포를 확인해 조정할 수
있다.

### Persona 프리셋 가중치 선정 이유
5개 프리셋(가격/공간/신축/지역 우선형, 균형형)의 가중치 숫자는 기획 문서
Phase 3 §12에 명시된 값을 그대로 채택했다 (`presets.ts`). 지역 우선형만 예외로,
"지역 필터를 강하게 적용"이라는 서술을 구현하기 위해 district 가중치를 나머지
항목 중 최댓값(가격 40)보다 높은 45로 설정해 "지역이 다른 조건보다 우선한다"는
의도를 가중치 크기로 표현했다. 언급되지 않은 차원(주택유형/층/유동성)은 0으로
두어 정규화 후 해당 프리셋에서는 영향을 주지 않는다.

### Trade-off Simulator 계산 방식
`simulateTradeoff(base, changed)` 하나의 순수 함수로 모든 시나리오(신축 조건
완화, 지역 고정 해제, 주택유형 확장, 최소면적 변경, 예산 변경)를 처리한다
(`tradeoff.ts`). 두 UserPreferences 세트 각각에 대해 동일한 필터
(`filterTransactions`)를 실거래 데이터에 적용한 뒤, 매칭 거래 수·매칭 지역
수·중앙 가격·중앙 면적·중앙 연식·㎡당가격을 비교한다. 조건 하나마다 별도 함수를
만들지 않고 "조건 완화 = 필터가 더 느슨한 UserPreferences로 바꿔 같은 함수를
호출"하는 구조로 통일해, 계산 로직이 시나리오별로 갈라지지 않게 했다. summary
문장은 실제로 값이 달라진 지표에 대해서만 생성한다(변화 없으면 문장도 없음).

## Phase 4 (Core UI/UX)

### 왜 추천/Trade-off 계산을 Server Action으로 호출하는가?
`data/processed/transactions.json`(25.7MB)은 서버 메모리에만 로드되어야 한다
(§30). `src/app/actions.ts`에 `"use server"` 함수(`runRecommendation`,
`runTradeoff`)를 두고 클라이언트는 결과 JSON만 받는다. 추가로 `src/lib/realestate/data.ts`
최상단에 `server-only` 패키지를 import해, 실수로 클라이언트 컴포넌트에서 이 모듈을
import하면 빌드 시점에 즉시 에러가 나도록 강제했다.

### 왜 URL query 대신 sessionStorage로 Assessment 입력값을 넘기는가?
예산·자기자본은 민감할 수 있는 금융정보라 URL에 노출하지 않는다 (§31).
`/assessment` 완료 시 `sessionStorage`에 저장하고, `/result`·`/tradeoff`·`/finance`가
마운트 시점에 읽어온다. 탭을 닫으면 사라지는 sessionStorage 특성이 "서버에 사용자
데이터를 저장하지 않는다"는 MVP 원칙과도 맞는다.

### 왜 Trade-off Simulator를 "조건별 탭 + 개별 옵션"으로 구현했는가?
기획이 요구한 5개 항목(예산/최소면적/건물연식/희망지역/주택유형)을 각각 다른
UI(프리셋 버튼 vs 드롭다운)로 다뤄야 해서, 연속 슬라이더 하나로 통일하기보다
탭으로 차원을 고르고 그 차원에 맞는 이산적 옵션을 보여주는 방식을 택했다.
서버 호출은 옵션 클릭 시점에만 발생해 드래그 중 과도한 요청을 피한다.

### BUY/WAIT/RENT&GROW 임계값(자기자본 30%/15%, DTI 40%)의 근거
데이터로 검증된 최적값이 아니라, 국내 시중은행이 통상 안내하는 "자기자본
30% 이상이면 대출 부담이 상대적으로 낮다"는 감각을 참고한 자체 휴리스틱이다
(`src/lib/finance/calc.ts`의 `classifyReadiness`). 정식 추천 알고리즘이 아니라
정보성 Concept Preview임을 UI 문구("참고용 개념 미리보기")로 항상 병기한다.

### 왜 컴포넌트에 `cn()`(clsx+tailwind-merge)을 도입했는가?
Landing "Data Trust" 카드에서 `<Card className="bg-navy text-white">`로 넘긴
색이 Card 자체의 기본 클래스(`bg-white`)에 가려 흰 배경에 흰 글자로 렌더링되는
버그가 실제로 발생했다. Tailwind 유틸리티 클래스의 우선순위는 클래스 문자열의
DOM 순서가 아니라 내부 스타일시트 생성 순서로 정해지므로, `clsx`로 base와
override를 단순 연결하면 override가 항상 이긴다는 보장이 없다. `tailwind-merge`는
같은 CSS 속성을 건드리는 클래스가 여럿이면 뒤에 오는 것만 남기므로 이 문제를
구조적으로 해결한다 (`src/lib/cn.ts`). Card/Button/Chip 등 `className`을
override 받는 모든 공통 컴포넌트에 적용했다.

## Phase 6 (QA & Polish)

### 발견한 버그: "자기자본 > 예산" 검증이 정상 케이스를 막고 있었다
Assessment Step1에 처음 추가한 검증 규칙("자기자본이 예산보다 크면 안 됨")이
개념적으로 틀렸다 — 자기자본이 예산보다 큰 것은 "대출 없이 전액 현금 구매"하는
정상적인 상황이다(`calculateFinance`도 `loanAmount = max(0, price-equity)`로 이
경우를 0원 대출로 자연스럽게 처리한다). 실제로 QA 시나리오(예산 5,000만원, 기본
자기자본 8,000만원)에서 이 검증이 "다음" 버튼을 영구히 비활성화시켜 사용자가
진행할 수 없는 실제 버그를 만들었다 — Playwright E2E 테스트로 발견했다. 해당
검증은 제거하고 `budget>0`, `cash>=0` 기본 검증만 남겼다.

### 왜 희망 지역 선택이 항상 추천 순위에 반영되지는 않는지 안내를 추가했는가
QA 중 `preferredDistricts=['용산구']`를 설정해도 지역 가중치를 0으로 두면(예:
"가격 우선형" 프리셋 선택 시) 용산구가 TOP5에 아예 안 나올 수 있음을 실제
데이터로 확인했다. 이는 Phase 3에서 의도적으로 설계한 동작이다 — 지역 적합도는
8개 점수 중 하나일 뿐이고, MVP는 "희망 지역 = 강제 필터"가 아니라 "희망 지역 =
점수 가중치"로 설계했다(§"지역 적합도" 참고). 알고리즘을 바꾸는 대신(Phase 6은
안정화 전용), `/assessment` STEP2에서 지역을 선택하면 "다음 단계에서 '지역'
중요도를 높여야 이 선택이 추천 순위에 우선 반영됩니다"라는 안내 문구를 추가해
사용자가 오해하지 않도록 했다.

### Trade-off 델타 표시에서 좋음/나쁨 색상 판단을 제거했다
Phase 4에서는 "면적 증가=초록, 가격 증가=빨강"처럼 방향에 따라 색을 입혔는데,
Phase 6 지침에 따라 이는 임의의 가치 판단이라 제거했다. `DeltaStat`은 이제
from→to와 실측 delta(`+38,157건`처럼 부호가 있는 실제 값)만 중립적인 색으로
보여준다 (`formatSigned*` 계열 함수, `src/lib/format.ts`).

### 색상 대비(WCAG AA) — 실측 후 별도 텍스트 전용 토큰을 추가했다
axe-core로 전 페이지를 자동 감사한 결과 `text-good`/`text-warn`/`text-bad`를
옅은 배경색(`*-bg`) 위 텍스트로 쓴 곳(신뢰도 배지, 금융 판정 카드 등)이 전부
4.5:1 기준에 미달했다(실측 2.47~3.59). 배경/아이콘/테두리용 밝은 톤과 텍스트용
어두운 톤을 분리해 `--color-good-text`(#15803d) 등 3개 토큰을 새로 추가했다
(대비 실측: good-text 4.76, warn-text 7.00, bad-text 5.91 — 모두 여유 있게
통과). `text-slate-400`도 흰 배경 대비 2.63으로 전부 미달이어서 실제 판독
텍스트에는 `text-slate-500`(4.76) 이상만 쓰도록 전면 교체했고, disabled 버튼처럼
WCAG가 대비 기준을 면제하는 비활성 컨트롤에만 slate-400을 남겼다.

### 키보드 입력 버그: 기본값이 있는 숫자 입력에 포커스 후 바로 타이핑하면 값이 이어붙었다
키보드 전용 내비게이션을 Playwright로 실측하다가 발견했다 — Tab으로 예산
입력창(기본값 30000)에 포커스한 뒤 "25000"을 입력하면 커서가 값 끝에 위치해
"2500030000"이 된다. 마우스 사용자는 보통 클릭 후 드래그/전체선택을 하지만
키보드 사용자는 이 문제를 그대로 겪는다. 모든 숫자 입력에 `onFocus={(e) =>
e.target.select()}`를 추가해 포커스 시 기존 값이 전체 선택되도록 했다.

### 왜 청년의 선호를 데이터에서 추론하지 않는가?
원본 실거래 데이터에는 구매자의 연령·구매 목적이 없다. "청년은 신축을 선호한다"
같은 문장은 데이터로 검증 불가능한 추측이며, 실제로는 특정 세대의 편향된 선택을
전체 청년의 선호로 일반화하는 오류로 이어질 수 있다. 대신 사용자가 `/assessment`
에서 직접 입력한 선호(중요도 가중치)를 실거래 데이터가 계산한 "그 선택의 비용"과
결합하는 구조로만 사용한다 (기획 문서 섹션 22).
